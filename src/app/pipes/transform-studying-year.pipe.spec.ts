import { TransformStudyingYearPipe } from './transform-studying-year.pipe';

describe('TransformStudyingYearPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformStudyingYearPipe();
    expect(pipe).toBeTruthy();
  });
});
